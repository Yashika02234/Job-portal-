// This is a placeholder file to satisfy the CSS processor
// All functionality has been moved to AboutUs.jsx

import { Navigate } from 'react-router-dom';

const Browse = () => {
  return <Navigate to="/about" />;
};

export default Browse; 